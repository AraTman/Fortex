import 'package:flutter/material.dart';

class ProductFinishedOrders extends StatelessWidget {
  const ProductFinishedOrders({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}